<script lang="ts">
	import { page } from '$app/state';

	// $effect(() => {
	// 	const path = page.route.id?.replace('[id]', `${page.params.id}/content`) ?? '';
	// 	window.location.replace(path);
	// });
</script>

<div>123</div>
